apiVersion: apps/v1
kind: Deployment
metadata:
  name: lens-template-service-python

spec:
  replicas: 1
  selector:
    matchLabels:
      app: lens-template-service-python
  template:
    metadata:
      labels:
        app: lens-template-service-python
    spec:
      containers:
      - name: lens-template-service-python
        image: ghcr.io/gravitate-health/lens-template-service-python:devserver
        imagePullPolicy: Always
        env:
        - name: ENVIRONMENT
          value: "prod"
        ports:
        - containerPort: 3000
          protocol: TCP
      restartPolicy: Always
status: {}
